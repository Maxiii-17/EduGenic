fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-dotenv>=1.0
google-genai>=1.0

# --- Optional: local explanation model (LaMini-Flan-T5-783M) ---
# Uncomment to enable fully-local concept explanations (needs ~3GB download + torch).
# transformers>=4.40
# torch>=2.0
